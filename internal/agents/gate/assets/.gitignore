# Embedded gate binaries are produced by `wick build` (or the
# CI release workflow) right before the main build that runs
# //go:embed. Do not commit them — checked-in copies would drift
# from source.
gate-*
*.exe
!.gitkeep
!.gitignore
