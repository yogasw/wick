package view

// SkillsSPAVM feeds the thin-shell that hosts the skills SPA
// inside the standard agents layout. AssetURL is the hashed Vite bundle
// path resolved at boot via spaAssetURL("skills").
type SkillsSPAVM struct {
	Layout   AgentsLayoutVM
	Base     string
	AssetURL string
}

// SkillsSPA renders the skills SPA shell inside the agents chrome.
// The SPA reads data-base from the #app div and uses it as the prefix for
// all API calls (e.g. ${base}/api/skills).
templ SkillsSPA(vm SkillsSPAVM) {
	@AgentsLayout(vm.Layout) {
		if vm.AssetURL == "" {
			<div class="p-8 text-sm text-rose-600 dark:text-rose-400">
				Skills bundle not built yet. Run
				<code class="font-mono">npm --workspace=@wick-fe/agents-skills run build</code>
				in <code class="font-mono">fe/</code> and refresh.
			</div>
		} else {
			<div
				id="app"
				data-base={ vm.Base }
				class="h-full"
			></div>
			<script type="module" src={ vm.AssetURL }></script>
		}
	}
}
